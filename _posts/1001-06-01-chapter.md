---
layout: post
---

代码

```javascript
function test() {
 console.log("哦， 我的天啊！");
}
```

```kotlin
companion object {
    fun launch(filmId: String, filmName: String) {
        if (filmId.isNullOrBlank() || filmName.isNullOrBlank()) {
            return
        }
        ARouter.getInstance().build("/movie/cinema")
        .withString("filmId", filmId)
        .withString("filmName", filmName)
        .navigation()
    }
}
```